# Source control & editor metadata
.git
.gitignore
.gitattributes
.github
.cursor
.vscode
.idea

# Node
node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Secrets & local env
.env
.env.*

# Local persistent data (never bake into the image)
data

# OS junk
.DS_Store
Thumbs.db

# Project-local docs/notes (not needed at runtime; docs PNGs shrink daemon upload)
TODO.md
README.md
docs/
demo/
*.log

# Compose files (not COPY'd into the image)
.dockerignore
docker-compose.yml
docker-compose.*.yml
